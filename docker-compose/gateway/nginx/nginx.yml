version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - 443:443
      - 80:80
    volumes:
      - /data/nginx/nginx.conf:/etc/nginx/nginx.conf
      - /data/https:/https
networks:
  default:
    name: zhanlai-network
    external: true